# __PROJECT_NAME__

NodomX starter created by `create-nodomx`.

Commands:

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

`npm run dev` starts a Vite development server with `.nd` single-file component support and NodomX HMR.
